{
  "allOf": [
    {
      "$ref": "#/components/schemas/integration"
    },
    {
      "type": "object",
      "properties": {
        "client_id": {
          "type": "string"
        },
        "client_secret": {
          "type": "string"
        },
        "webhook_secret": {
          "type": "string"
        },
        "pem": {
          "type": "string"
        }
      },
      "required": [
        "client_id",
        "client_secret",
        "webhook_secret",
        "pem"
      ],
      "additionalProperties": true
    }
  ]
}